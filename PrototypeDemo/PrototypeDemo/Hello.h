#pragma once

//Function prototype for SayHello
//Basically, a preview containing
// -- the function name
// -- the function return type
// -- the parameter list
void SayHello(void);

//You only NEED parameter types for proto-types
void SayHello(char*);
// void SayHello(char* name); would also be fine
